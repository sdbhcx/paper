# 2026-08-07

- 在 `E:\project\my\geal_test` 扩展了 GEAL 评测：`utils/metrics.py` 新增与原 GEAL 20 阈值协议一致的 aIoU、固定阈值 IoU、小区域 aIoU/召回、KNN 点云 Boundary F-score、假阳性点数/比例、affordance 面积分桶、基于平均最近邻距离的密度分桶，以及参数量、延迟、CUDA 峰值显存和 profiler FLOPs 估计。
- 新增 `scripts/evaluation_extended.py`，输出总体 JSON 与逐样本 JSONL；新增 `scripts/evaluation_corrupt_extended.py`，保存七类 corruption 的逐 severity 曲线、均值和归一化 AUSC；新增 `scripts/evaluation_density.py`，对同一样本按 2048/1536/1024/512 等点数做确定性随机降采样并报告多次采样均值/标准差。
- 更新项目 README，补充扩展评测运行命令和统计口径。三个 Python 文件已通过 `py_compile` 语法检查；当前会话缺少与项目匹配的 PyTorch/数据集/权重环境，因此未运行端到端模型评测。
- 重要统计约定：小区域默认定义为真值正点比例 ≤5%；Boundary F-score 用 KNN 邻域标签变化提取边界；源码没有网格面面积，因此“假阳性面积”实现为假阳性点数及其点比例代理；密度用归一化点云的平均最近邻距离衡量。
- 完成 GEAL Unseen 泛化专题文献简报：核验 LASO、IAGNet、OpenAD、GEAL、GREAT、LMAffordance3D、Aff3DFunc、Affogato、QueryMe、TASA 等论文/页面，整理问题诊断、论文证据分层、Affogato Table 2/3 数字和 GEAL 的 P0-P5 改进路线；结果保存为 `outputs/GEAL-Unseen泛化问题与相关论文研究简报.md`。
